  [alias]

    co = checkout

    st = status

    cm = commit -m

    br = branch

    bm = branch -m

    bd = branch -D

    cb = checkout -b

    df = diff

    ls = log --stat

    lp = log -p

    plo = pull origin

    plode = pull origin develop

    pho = push origin

    au = add -u .

    cm = commit

    pf = push

[user]
    email = qingshui@foxmail.com
    name = qingshui

[core]
   editor = nvim

